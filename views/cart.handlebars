<!DOCTYPE html>
<html>
<head>
  <title>Carrito</title>
</head>
<body>
  <h1>Carrito</h1>
  <ul>
    {{#each cart.items}}
      <li>{{this.name}} - {{this.price}}</li>
    {{/each}}
  </ul>
</body>
</html>