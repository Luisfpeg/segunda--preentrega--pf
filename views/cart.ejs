<!DOCTYPE html>
<html>
<head>
  <title>Carrito</title>
</head>
<body>
  <h1>Carrito</h1>
  <ul>
    <% cart.items.forEach(function(item) { %>
      <li>Product ID: <%= item.productId %>, Quantity: <%= item.quantity %></li>
    <% }); %>
  </ul>
</body>
</html>